services:
  - type: web
    name: mevalur-shree-nagaraja-kshetram
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.20.0
